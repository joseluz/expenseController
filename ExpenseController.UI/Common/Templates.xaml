<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExpenseController.UI.Common">

    <DataTemplate x:Key="CategoryTemplate">
        <Grid HorizontalAlignment="Left" Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                <Image Source="{Binding Image}" Height="40" Width="40" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel Margin="0,0,0,0" Orientation="Vertical">
                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource ApplicationForegroundThemeBrush}" Style="{StaticResource ItemTextStyle}" Width="200" VerticalAlignment="Center" Text="{Binding Name}" HorizontalAlignment="Left" FontFamily="Segoe UI" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
